import React from 'react';
import ReactDOM from 'react-dom';
import { Link } from 'react-router-dom';

const LandingPage = () => (
  <div>
    <Link to='loginForm'>BEGIN (send to loginForm)</Link>
  </div>
)

export default LandingPage;
